---
import ListItem from "./ListItem.astro";
import Divider from "../Divider.astro";
import type { ListItemProps } from "./ListItemProps";

interface Props {
  items: ListItemProps[];
}
const { items } = Astro.props;
---

<<<<<<< HEAD
<div class="flex flex-col items-center gap-2 px-4 py-10">
  =======
  <div class="flex flex-col items-center gap-2 px-4 py-10">
    >>>>>>> fdab4d5fdf5fddd312fe462bbfd9b1841eb5a258
    {
      items.map((item, i) => (
        <>
          <ListItem {...item} />
          {i < items.length - 1 && <Divider />}
        </>
      ))
    }
  </div>
</div>
