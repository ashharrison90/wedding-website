---
interface Props {
  id: string;
  type: "submit" | "button";
  text: string;
}

const { id, type, text } = Astro.props;
---

<button
  id={id}
  class="bg-secondary disabled:bg-secondary--alt text-canvas--alt cursor-pointer rounded-md px-5 py-2 no-underline"
  type={type}>{text}</button
>
