---
import "../styles/global.css";
import NavBar from "../components/NavBar/NavBar.astro";
import DiscoBall from "../components/DiscoBall.astro";
import Divider from "../components/Divider.astro";
import Date from "../components/Date.astro";
import Hero from "../components/Hero.astro";

interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <NavBar />
    <div class="px-4 pb-4">
      <main class="mx-auto max-w-6xl pt-4">
        <div
          class="flex flex-wrap items-center justify-center gap-y-4 justify-self-center pb-4"
        >
          <div class="flex flex-col items-center gap-4">
            <Hero />
            <Date />
          </div>
          <DiscoBall />
        </div>
        <div class="flex flex-col items-center py-4">
          <h1 class="mb-10 font-serif text-3xl md:text-5xl">{title}</h1>
          <Divider size="large" />
          <slot />
        </div>
      </main>
    </div>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    min-height: 100%;
  }
</style>
