---
interface Props {
	question: string;
  answer: string;
}
const { question, answer } = Astro.props;
---
<div class="flex flex-col gap-2 p-2">
  <h2 class="font-bold font-sans text-2xl">{question}</h2>
  <p class="font-serif">{answer}</p>
</div>