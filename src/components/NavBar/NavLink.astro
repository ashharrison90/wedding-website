---
interface Props {
  href: string;
  text: string;
}
const { href, text } = Astro.props;
const isActive = Astro.url.pathname === href;
---
<a href={href} class=`${isActive ? 'underline' : 'no-underline'} uppercase text-xs text-contrast hover:text-emerald-700`>{text}</a>